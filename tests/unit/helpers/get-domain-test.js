
import { getDomain } from 'hn-emberjs/helpers/get-domain';
import { module, test } from 'qunit';

module('Unit | Helper | get domain');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getDomain([42]);
  assert.ok(result);
});

